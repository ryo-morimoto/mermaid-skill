{
  "name": "mermaid-skill",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "bun build src/validate.ts --outfile plugins/dist/validate.mjs --target=node --minify"
  },
  "devDependencies": {
    "@types/bun": "^1.2.9",
    "mermaid-ast": "^0.8.2"
  }
}
